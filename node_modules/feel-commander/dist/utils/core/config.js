"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const tslib_1 = require("tslib");
const os = tslib_1.__importStar(require("os"));
const path = tslib_1.__importStar(require("path"));
const worker_process_1 = require("../worker-process");
exports.defaultFeelPath = path.join(os.homedir(), '.feel');
exports.defaultFeelPm2Path = `${exports.defaultFeelPath}/pm2`;
exports.defaultFeelInstancePath = `${exports.defaultFeelPath}/instances`;
exports.defaultBackupPath = `${exports.defaultFeelPath}/backups`;
const NODE_BIN = './bin/node';
exports.getFeelConfig = async (installDir, network) => {
    const cmd = `${NODE_BIN} scripts/generate_config.js -n ${network} | head -n 10000`;
    const kb = 1024;
    const size = 400;
    const maxBuffer = kb * size;
    const { stdout, stderr } = await worker_process_1.exec(cmd, { cwd: installDir, maxBuffer });
    if (stderr) {
        throw new Error(stderr);
    }
    return JSON.parse(stdout);
};
//# sourceMappingURL=config.js.map