import { NETWORK } from '../constants';
interface AppConfig {
    readonly version: string;
    readonly minVersion: string;
    readonly protocolVersion: string;
}
interface StorageConfig {
    readonly database: string;
    readonly user: string;
    readonly password: string;
}
interface CacheConfig {
    readonly password: string;
    readonly enabled: boolean;
}
interface ComponentsConfig {
    readonly storage: StorageConfig;
    readonly cache: CacheConfig;
}
export interface FeelConfig {
    readonly app: AppConfig;
    readonly components: ComponentsConfig;
}
export declare const defaultFeelPath: string;
export declare const defaultFeelPm2Path: string;
export declare const defaultFeelInstancePath: string;
export declare const defaultBackupPath: string;
export declare const getFeelConfig: (installDir: string, network: NETWORK) => Promise<FeelConfig>;
export {};
